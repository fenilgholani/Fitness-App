<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthapp">

    <!--  Permissions necessary for Firebase  -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthApp">

        <activity android:name=".BetterEx"
            android:theme="@style/Theme.HealthApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignInActivity" />

        <activity
            android:name=".MainActivity">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->
<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>


        <activity android:name=".InfoActivity" ></activity>
        <activity android:name=".RecordActivity" ></activity>
        <activity android:name=".ExerciseInfo" ></activity>
        <activity android:name=".TodayExerciseActivity"></activity>
        <activity android:name=".TimerActivity"></activity>
        <activity android:name=".CalendarActivity"></activity>
<!--        <activity android:name=".BetterEx"></activity>-->
        <activity android:name=".SettingActivity"></activity>
    </application>

</manifest>